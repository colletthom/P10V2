@page "{id?}"
@model Front.Pages.Note.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create d'une note</h1>
<h4>pour le patient: @Model.creationNoteVM.patient</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <input asp-for="creationNoteVM.patId" type="hidden" />
            <input asp-for="creationNoteVM.patient" type="hidden" />
            <input asp-for="creationNoteVM.note" type="hidden" />
          @*  <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="creationNoteVM.note" class="control-label">Note à renseigner</label>
                <input asp-for="creationNoteVM.note" class="form-control" />
                <span asp-validation-for="creationNoteVM.note" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
