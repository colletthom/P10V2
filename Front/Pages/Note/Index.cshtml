@page
@model Front.Pages.Note.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoteVM[0].patId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoteVM[0].patient)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoteVM[0].note)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.NoteVM) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.patId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.patient)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.note)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
